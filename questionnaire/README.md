# Questionnaire

1. Open `index.html` in your browser
2. Fill out all sections
3. Click **"Generate Answers File"** — this downloads `questionnaire_answers.json`
4. Move the downloaded file to the `answers/` directory in this repo
5. Commit and push
